# 2026年9月23日

## 1.4.3

### 修复

- 补齐节假日接口的 User-Agent 常量。修复了引用未定义的名称而抛错并被静默跳过，导致「节假日感知」不生效问题。

### 变更

- 计算下一次触发时间时纳入非随机的被动触发器：调度器与页面预览直接给出明确的最早的可触发时刻，不再让短周期主动触发器在未激活区间反复到点并写入大量「未通过」运行日志。随机型仍不可预测，不参与计算。

# 2026年9月22日

## 1.4.2

- 删除 `poll_interval` 配置。每个任务直接等待下一个未来主动触发点，编辑任务时仍会立即唤醒并重新计算。

## 1.4.1

- 主动触发器无记录：不再保存或继承 `last_fired`，每次都以当前系统时间计算下一个未来触发点；启动、重载、编辑和执行结束后不会补处理历史时间点。被动-冷却型仍使用任务级成功时间。

## 1.4.0

- 将插件拆为入口与 Web API、调度运行层、任务执行器和存储层；保留纯计算核心。所有本地组件在插件加载时按路径重新加载，避免同名旧模块缓存导致签名不匹配。
- 每个启用任务独立等待、检查和执行；慢任务与故障不再拖延其他对话。手动和自动执行互斥，同一对话的本插件执行串行。
- 非法配置和未预期异常使受影响任务进入可见的故障状态，编辑保存恢复。
- 触发点在发送前持久化，发送失败或超时不自动重发。冷却时间从实际成功完成时计算。存储按快照串行写入，卸载等待写入线程结束。
- **不兼容变更**：任务改用单个 `target` 对话字符串，取消多目标选择和 `targets` 保存接口。旧多目标任务只保留第一个目标并停用，等待用户重新配置；旧单目标任务保留启用状态。新运行日志使用 `target_result`，旧日志仍可查看。
- 页面展示等待、执行、故障和待配置状态，并定期刷新。`poll_interval` 改为“最长等待复查间隔”，默认 600 秒（10 分钟）：各任务最多等待此时长就检查自己的主动触发器是否到点，触发点更近或任务被编辑时提前唤醒，不改变任务执行周期。

# 2026年9月21日

## 1.3.3

### 变更

- **运行日志拆分**：日志从 `tasks.json` 移出，独立存为 `run_logs.json`，任务文件只保留任务定义。`get_data` 不再全量下发日志，新增 `GET /get_run_logs` 按页拉取（默认每页 20 条、最新在前），页面切到运行日志页时才请求。
- 运行日志保留条数默认值由 200 调整为 100。

### 兼容

- 启动时自动把旧版 `tasks.json` 里的 `logs` 迁移到 `run_logs.json`，并从任务文件中移除该字段。

## 1.3.2

### 新增

- 任务编辑弹窗新增「JSON 模式」：弹窗右上角切换，可查看并直接编辑整条任务的 JSON，结构为 `{id?, name, enabled, triggers, content, targets}`，与 `upsert_task` 接口同构，各触发器的 `config` 也在其中。保存走与表单相同的校验，非法配置不会入库；切回表单模式会按 JSON 内容回填，解析失败时提示错误并留在 JSON 模式。

### 文档

- README 重写：按安装、任务组成、触发器与调度行为、任务内容、JSON 格式、页面用法、日志与存储的顺序重新组织，新增「任务 JSON 格式」章节完整说明字段与取值约束。

## 1.3.1

### 修复

- 修正 LLM 调用日志中图片徽标的解析

### 变更

- 「触发一次」按钮点击后进入禁用 + 加载态（转圈 + 「触发中…」），完成后恢复，避免连点造成重复触发。
- 任务编辑页显示优化
- 触发器配置面板UI优化
- 运行日志改为每页 20 条分页浏览。

## 1.3.0

### 变更

- **取消逻辑规则表达式**：触发组合改为固定语义，并提供图形化交互。
- **触发器改为图形化框图**：`Start` → 并排的主动型节点（或汇聚）→ 串行的被动型节点（且串联）→ `Target`。
- **任务编辑页重组**：任务名称 / 启用开关 / 发送对象 / 触发器框图 / 任务内容分区排列，移除逻辑规则输入框与模板变量按钮区。
- **任务提示词标准化包裹**：两种 AI 模式的用户提示词将被插件自动统一包裹为 `<scheduled_task>这是由计划任务自动触发的一次会话[，现在时间是 年月日 周几 时分秒][，今天是 …]。请根据以下指令进行回复。你的回复将被直接发送给用户。{{task_prompt}}</scheduled_task>`；新增「时间感知」开关控制是否注入时间（固定文本模式该开关禁用）。
- **节假日感知**：新增开关，开启后通过 `https://timor.tech/api/holiday/info/<日期>` 查询并注入工作日/周末/节日/调休信息（如「今天是工作日」「今天是“中秋节”假期」「今天是工作日，是“国庆节后补班”」）；按天缓存，查询失败自动跳过、不影响发送。
- **提示词参数取消**：不再允许用户操作提示词参数。
- 任务列表移除「逻辑规则」列，触发器列改为「N 主动 / N 被动」；运行日志表头「逻辑结果」改为「条件结果」。

### 兼容

- 旧数据自动迁移：启动时删除任务中的 `logic_expr`（按新固定语义运行），补齐内容开关 `time_aware`（默认开启）与 `holiday_aware`（默认关闭），并把等于旧版默认值的「独立 AI 系统提示」（`<proactive_trigger>…`）清空 —— 该包裹文本已由任务提示词外层统一承担。

## 1.2.0

### 新增

- LLM 调用日志改为「一条记录一个文件」：文件名以请求时间开头（含任务名与序号），按文件名排序即时间序，无需额外索引；每个文件首行即元数据，列表只读首行，单页响应由 MB 级降到 KB 级。
- 记录中的 base64 图片在写入时自动抽离为独立图片文件并按内容 md5 寻址：同一图片在不同记录、不同任务间只存一份，只在点击图片徽标时才加载（`get_llm_image`）。
- 日志页改为每页 10 条分页：先一次性渲染整页折叠卡片骨架（时间、任务名、模式、UMO、体积、耗时、预览），再逐条串行加载正文，加载一条即就绪一条、可展开一条。
- 正文只在卡片首次展开时才渲染进页面，避免多条大体积文本同时渲染造成卡顿。
- 浏览器端按内容缓存图片与已加载正文：翻回看过的页、重复出现的图片均不再请求；「清空」时一并清除。
- 旧版单文件 `llm_calls.jsonl` 在启动时自动拆分为逐条记录文件并抽离图片，迁移完成后原文件改名保留为 `llm_calls.jsonl.migrated`。

### 变更

- `/get_llm_logs` 语义变更为轻量元数据分页（默认 `limit=10`），不再返回完整请求/回复正文。
- 新增 `/get_llm_log_detail`（按文件名取单条正文）与 `/get_llm_image`（按内容寻址取图片）。
- 过期管理：裁剪最旧的记录文件后，回收不再被任何记录引用的图片。

# 2026年9月11日

## 1.1.0

### 新增

- LLM 调用日志改为按条折叠卡片：卡片显示时间 + 前 200 字符摘要，点击展开查看完整请求与回复。
- 日志内容支持「原始格式 / 格式化」一键切换（默认原始格式，不自动格式化）。
- 请求体中的 base64 图片折叠为「base64图片」标签，点击即可查看原图，避免日志臃肿。
- 切换「LLM 调用日志」页面时显示加载中指示器，不再先显示「暂无记录」。


# 2026年9月5日

## 1.0.0

- 初版发布：未来计划任务调度器。
- 五种触发器：周期型（interval）、cron 型、区间型（window）、随机型（random）、冷却型（cooldown）；通过逻辑规则表达式（`+` 或、`*` 且、括号）组合触发器组。
- 任务内容三种模式：固定文本 / 独立 AI / 对话 AI；支持 `{{time}}` / `{{date}}` / `{{weekday}}` 及触发器结果 `{{n}}` 模板变量。
- 自适应调度：按最近触发点唤醒，宕机不补发。
- WebUI 管理页：任务增删改查、逻辑规则实时校验、运行日志、LLM 调用日志、插件配置。
- 持久化：`tasks.json`、`llm_calls.jsonl`。
